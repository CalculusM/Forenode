name: keep-streamlit-awake

# 주의: GitHub 크론은 지연·결번이 잦아 6시간 주기로는 수면 임계를 넘길 수 있음('26-09-02 실측).
# 2시간 주기 = 한 번 결번돼도 실효 간격 4~5시간. 저장소가 60일간 커밋 없으면 스케줄이
# 자동 비활성화되므로(마지막 push 기준) 두 달에 한 번은 커밋이 필요함.
on:
  schedule:
    - cron: '23 */2 * * *'
  workflow_dispatch:

jobs:
  visit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Install Playwright
        run: |
          npm init -y > /dev/null
          npm install playwright > /dev/null
          npx playwright install --with-deps chromium > /dev/null

      - name: Visit app and wake if sleeping
        run: |
          cat > keepalive.mjs <<'EOF'
          import { chromium } from 'playwright';
          const url = 'https://forenode.streamlit.app/';
          const browser = await chromium.launch();
          const page = await browser.newPage();
          await page.goto(url, { waitUntil: 'domcontentloaded', timeout: 90000 });
          await page.waitForTimeout(15000);
          const wake = page.getByText(/back up/i).first();
          try {
            await wake.waitFor({ state: 'visible', timeout: 5000 });
            console.log('App asleep - clicking wake button');
            await wake.click();
            await page.waitForTimeout(120000);
            console.log('Wake clicked; app booting');
          } catch {
            console.log('App awake - visit registered');
          }
          console.log('Final title:', await page.title());
          await browser.close();
          EOF
          node keepalive.mjs
